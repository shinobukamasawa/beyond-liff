# このフォルダは写しです

`Membership.js` と `Teian.js` は、`gas/logic/` にある正本を **1文字も変えずに** 複写したものです。ブラウザの中で、GAS と同じ提案の計算をするために置いています。

- 直すのは `gas/logic/` のほうです。ここは直さないでください
- 直したら `npm run sync:logic` で写しを作り直します
- ずれたままだと `npm test`・`npm run push`・`npm run deploy:web` が止まります
